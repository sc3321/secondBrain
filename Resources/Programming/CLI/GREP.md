Global Regular Expression Print

Like CTR + F on steroids.

**Core usage:**

`grep "needle" haystack.txt      # Search for "needle" in file` 
`grep -r "foo" .                 # Recursively search for "foo" in current dir` 
`grep -i "bar" file.txt          # Case-insensitive search`