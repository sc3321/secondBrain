Concatenate.

Concatenates and displays file contents.

**Mental model:**  
Think: “show me the whole scroll, unrolling it in a location that I specify, default being the terminal”

**Core usage:**

`cat file.txt           # Dump file to terminal` 
`cat file1 file2 > out  # Combine files and write to 'out'`